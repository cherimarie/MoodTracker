<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    
        
               <!-- Chili Lime Theme -->
    <style name="cl_heading1">
        <item name="android:textSize">@dimen/h1</item>
        <item name="android:textColor">@color/chilis</item>
        <item name="android:background">@color/sand</item>
    </style>
    
    <style name="cl_heading2">
        <item name="android:textColor">@color/cilantro</item>
        <item name="android:textSize">@dimen/h2</item>
    </style>

    <style name="cl_para">
        <item name="android:textSize">@dimen/para</item>
    </style>
    
    <style name="cl_error">
        <item name="android:textColor">@color/error</item>
    </style>
    

</resources>